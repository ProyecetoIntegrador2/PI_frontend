"use client";

import React from "react";

const GeneratePDF = () => {
  const handleExportPdf = () => {
    // Funcionalidad de exportar PDF (pendiente de implementación)
  };

  return (
    <div>
      <button onClick={handleExportPdf}>Generar PDF</button>
    </div>
  );
};